services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      front:
        condition: service_completed_successfully

  front:
    image: star1431/next-skillary:latest # 임시 테스트 - seonghun120614 -> star1431
    build:
      context: ./next-skillary-front
      dockerfile: Dockerfile
    env_file: .env.front
    depends_on:
      back:
        condition: service_completed_successfully

  back:
    image: star1431/spring-skillary:latest  # 임시 테스트
    build:
      context: ./spring-skillary-back
      dockerfile: Dockerfile
    env_file: .env.back
    depends_on:
      db:
        condition: service_healthy

  db:
    image: star1431/mysql-skillary:latest  # 임시 테스트
    env_file: .env.db
    volumes:
      - db_data:/var/lib/mysql
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 512M
        reservations:
          cpus: '1'
          memory: 256M
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 5s

volumes:
  db_data: